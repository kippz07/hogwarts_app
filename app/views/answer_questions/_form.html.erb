<%= form_with(model: answer_question, local: true) do |form| %>
  <% if answer_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(answer_question.errors.count, "error") %> prohibited this answer_question from being saved:</h2>

      <ul>
      <% answer_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
