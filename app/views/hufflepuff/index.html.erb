<div id="huff-body">
  <h1>Hufflepuff Common Room</h1>
  <div class="left">
    <h3>Members</h3>
    <% @users.each do |user| %>
      <p onclick="window.location = '/great_hall/hufflepuff/<%= @users.index(user) + 1 %>'"><%= user.name %></p>
    <% end %>
  </div>

  <div class="right">
    <h3>Hufflepuff Spells</h3>
    <% @indi_spells.each do |spell| %>
      <p><%= spell.name %> - <%= spell.catagory %></p>
    <% end %>
  </div>
</div>